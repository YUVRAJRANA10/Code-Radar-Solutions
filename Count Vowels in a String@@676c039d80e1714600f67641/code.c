#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    // printf("%s", welcome());
    char cc[100];
    scanf("%s",&cc);
    printf("%s",cc);
    return 0;
}