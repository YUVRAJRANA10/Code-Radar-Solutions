#include <stdio.h>
#include<ctype.h>


int main() {
    // printf("%s", welcome());
    char cg;
    scanf("%c",&cg);
    char cj = toupper(cg);
    char cl = tolower(cg);
    
    if(isdigit(cg){
        printf("Not an alphabet");
    })
    else if(cg == cj){
        printf("Uppercase");
    }
    else if(cg == cl ){
        printf("Lowercase");
    }
  
    return 0;
}