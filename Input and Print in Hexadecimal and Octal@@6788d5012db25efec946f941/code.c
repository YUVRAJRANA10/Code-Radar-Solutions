#include <stdio.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    // printf("%s", welcome());
    int a;
    scanf("%d",&a);
    printf("Hexadecimal: %c\n Octal: %d",Hexadecimal(a),Octal(b));
    return 0;
}