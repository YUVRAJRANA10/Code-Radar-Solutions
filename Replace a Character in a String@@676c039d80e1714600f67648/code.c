// Your code here...
#include <stdio.h>
#include <string.h>

int main() {
    char cc[100];
    char re,rep;
    fgets(cc, sizeof(cc), stdin);
    scanf("%c",&re);
    scanf("%c",&rep);
 char bb[100] == cc;
    int counter = 0;
    for (int i = 0; i < strlen(cc); i++) { 
    // char bb[100] == cc[i];
         }
    printf("%s\n",bb );
    return 0;
}
