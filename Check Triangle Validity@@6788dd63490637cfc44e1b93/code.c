#include <stdio.h>
#include<math.h>

// char* welcome() {
//     return "Welcome to Code Radar!";
// }

int main() {
    // printf("%s", welcome());
    int a,b,c;
    scanf("%d %d %d",&a,&b,&c);
    if(pow(a,2)+ pow(b,2) = pow(c,2)){
        printf("Valid");
    }
    else{
        printf("Invalid");
    }
    return 0;
}