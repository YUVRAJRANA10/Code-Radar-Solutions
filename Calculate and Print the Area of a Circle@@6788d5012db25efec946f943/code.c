#include <stdio.h>
#include<math.h>

char* welcome() {
    return "Welcome to Code Radar!";
}

int main() {
    // printf("%s", welcome());
    float r;
    scanf("%f",r);
    float pie = 3.14;
    float area = pir*r*r;
    printf("Area: %.2f",area);
    return 0;
}